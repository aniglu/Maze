export enum ActionSignal {
    ArrowUp = 'ArrowUp',
    ArrowDown = 'ArrowDown',
    ArrowRight = 'ArrowRight',
    ArrowLeft =  'ArrowLeft'
}

export enum Signal {
    Click = 'click',
    Keydown = 'keydown'
}